export const giscusConfigs = {
  repo: "kwei/Notes" as `${string}/${string}`,
  repoId: process.env.DATA_REPO_ID ?? "",
  category: "Announcements",
  categoryId: process.env.DATA_CATEGORY_ID ?? "",
};
